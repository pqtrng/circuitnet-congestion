# Generated by tests/test_prose_claims.py. Do not edit entries by hand;
# move a line between sections to reclassify it, delete it once the
# source line no longer carries a quantity.

# INVARIANT -- cannot drift: architecture, format, standards, fixtures.
README.md::**Model.** U-Net, depth 4, 7.8M parameters, bilinear upsampling, linear output head. Trained in single precision for 60
README.md::**Patches** are 128×128 crops at native resolution, never resized: congestion is a density on a physical routing grid,
README.md::- A 128×128 patch gives a local receptive field. Congestion driven by global layout structure beyond one patch is
README.md::Model weights are excluded from the repository. Every run record stores a SHA-256 per checkpoint alongside the git
analysis/probe_throughput.py::# A per-image time this many times the flat-region median is treated as spill
docs/data_decisions.md.tmpl::resolution. Patch size 128 was chosen over 256 because it requires no padding for
docs/data_decisions.md.tmpl::samples -- the exact shares are rendered below -- and 128 yields a substantially
docs/data_decisions.md.tmpl::the smallest chips, while a 256 window exceeds the smaller dimension of most
docs/training_decisions.md.tmpl::0.3 and value 0.07, values chosen for the arithmetic rather than measured --
docs/training_decisions.md.tmpl::a SHA-256 per checkpoint, the git revision, and whether the worktree was clean —
docs/training_decisions.md.tmpl::demand exceeding capacity by more than five percent, and the level sitting
docs/training_decisions.md.tmpl::exactly at five percent is excluded by a strict comparison. And the concentration
src/circuitnet_congestion/data/provenance.py::# UTC ISO-8601
src/circuitnet_congestion/data/silver.py::# (integer 0 -> float 0.0); this only normalizes the storage dtype.
src/circuitnet_congestion/models/unet.py::128 -> 64 -> 32 -> 16 -> 8, so a bottleneck unit summarises a 16x16 input
src/circuitnet_congestion/models/unet.py::Default width is deliberately modest (base 32, ~7.8M parameters). The
src/circuitnet_congestion/training/dataset.py::# multiplicatively and summed over 16384 pixels, which overflows an
src/circuitnet_congestion/training/dataset.py::features : float32 [3, 128, 128] z-scored (cell_density, rudy) + binary macro_region
src/circuitnet_congestion/training/dataset.py::gt : float32 [128, 128] raw congestion = max(horizontal, vertical)
src/circuitnet_congestion/training/dataset.py::mask : uint8 [128, 128] 1 = real die area, 0 = edge zero-padding
tests/test_dataset.py::The mask is cast at read time: it is summed over 16384 pixels downstream,
tests/test_gold.py::# mean over VALID pixels only = 10.0 (not diluted by padded zeros)
tests/test_metrics.py::probability 0.3 and value 0.07. This minimises squared error and detects
tests/test_silver.py::# gt = max(0, 0.3) = 0.3, no crash from int dtype
tests/test_silver.py::# max(0.5, 0.3)

# PENDING -- measurements to relocate. Must reach zero.
README.md::The zero-predictor error spans a factor of 10.5 across splits, so **absolute error is not comparable between them**.
README.md::This is not a subtle effect. It follows directly from the target: 98.5% of valid pixels are zero, so a predictor that
README.md::with Pandera schema validation and SHA-1 fingerprinting. Deduplication reduced 10,606 nominal samples to 4,216 unique
README.md::| `tests/` | 115 tests, CPU-only, synthetic fixtures |
README.md::| plain squared error | highest F1 | 41 | **1.001** | **0.0356** |
README.md::| plain squared error | lowest error | 5 | 0.949 | 0.0022 |
README.md::| test | openc910-1 | 3,657 | 5.575% | 1.36e-04 |
README.md::| train | 4 designs | 37,704 | 1.601% | 3.07e-05 |
README.md::| val | Vortex-large | 6,641 | 0.955% | 1.30e-05 |
README.md::| weighted | highest F1 | 7 | 1.296 | **0.0740** |
README.md::| weighted | lowest error | 9 | 1.187 | 0.0503 |
tests/test_losses.py::a meaningful gap. The level 1/20 is exactly five percent and the strict
tests/test_losses.py::percent. Levels around 0.05 are dense -- between three hundred and six
tests/test_losses.py::without it, given that ~98.5% of valid target pixels are zero.
